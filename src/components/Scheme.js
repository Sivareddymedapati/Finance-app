import React, { Component } from 'react'

class Scheme extends Component {
    render() { const {schem} = this.props;
        return (
            <div>
                {this.props.sch}
            </div>
        )
    }
}

export default Scheme
